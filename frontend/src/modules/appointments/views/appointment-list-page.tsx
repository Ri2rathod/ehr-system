"use client";

import { useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import { useAppointments } from "../hooks/use-appointments";
import { AppointmentStatus, VisitType } from "../types/appointment.types";
import { toLocalDate } from "../utils/appointment-format";
import { AppointmentTable } from "../components/appointment-table";

const statuses: AppointmentStatus[] = ["SCHEDULED", "CONFIRMED", "CHECKED_IN", "IN_PROGRESS", "COMPLETED", "CANCELLED", "NO_SHOW"];
const visitTypes: VisitType[] = ["CONSULTATION", "FOLLOW_UP", "EMERGENCY", "TELEMEDICINE", "PROCEDURE", "VACCINATION"];

export default function AppointmentListPage() {
  const router = useRouter(); const [status, setStatus] = useState<AppointmentStatus | "">(""); const [visitType, setVisitType] = useState<VisitType | "">(""); const [dateFrom, setDateFrom] = useState(""); const [dateTo, setDateTo] = useState(""); const [page, setPage] = useState(0); const [size, setSize] = useState(20);
  const filters = useMemo(() => ({ status: status || undefined, visitType: visitType || undefined, dateFrom: dateFrom || undefined, dateTo: dateTo || undefined, page, size, sortBy: "startTime", sortDir: "asc" as const }), [status, visitType, dateFrom, dateTo, page, size]);
  const { data, isLoading, isError } = useAppointments(filters); const items = data?.items || []; const totalPages = data?.totalPages || 1;
  const setRange = (range: "today" | "tomorrow" | "week") => { const now = new Date(); if (range === "tomorrow") now.setDate(now.getDate() + 1); const from = toLocalDate(now); const to = new Date(now); if (range === "week") to.setDate(now.getDate() + 6); setDateFrom(from); setDateTo(toLocalDate(to)); setPage(0); };
  const count = (value: AppointmentStatus) => items.filter((item) => item.status === value).length;
  return <div className="space-y-4 p-5"><header className="flex flex-wrap items-start justify-between gap-3"><div><h1 className="text-xl font-bold text-on-surface">Appointments</h1><p className="text-xs text-on-surface-variant">Coordinate visits, arrivals, and clinical flow.</p></div><button onClick={() => router.push("/appointments/new")} className="h-9 rounded-md bg-primary px-3 text-xs font-bold text-on-primary">+ New Appointment</button></header><section className="grid grid-cols-2 gap-2 md:grid-cols-5">{[["Today", items.length], ["Scheduled", count("SCHEDULED")], ["Checked in", count("CHECKED_IN")], ["In progress", count("IN_PROGRESS")], ["Completed", count("COMPLETED")]].map(([label, value]) => <div key={String(label)} className="rounded-md border border-outline-variant bg-surface-container-lowest p-3"><p className="text-[10px] font-bold uppercase tracking-wider text-on-surface-variant">{label}</p><p className="mt-1 text-lg font-bold text-on-surface">{value}</p></div>)}</section><section className="flex flex-wrap items-end gap-2 rounded-lg border border-outline-variant bg-surface-container-lowest p-3"><div><label className="block text-[10px] font-bold uppercase tracking-wide text-on-surface-variant">Date</label><div className="mt-1 flex gap-1"><button onClick={() => setRange("today")} className="rounded border border-outline-variant px-2 py-1 text-xs">Today</button><button onClick={() => setRange("tomorrow")} className="rounded border border-outline-variant px-2 py-1 text-xs">Tomorrow</button><button onClick={() => setRange("week")} className="rounded border border-outline-variant px-2 py-1 text-xs">This week</button></div></div><label className="text-xs text-on-surface-variant">From<input aria-label="Start date" type="date" value={dateFrom} onChange={(e) => { setDateFrom(e.target.value); setPage(0); }} className="mt-1 block h-9 rounded border border-outline-variant bg-surface px-2" /></label><label className="text-xs text-on-surface-variant">To<input aria-label="End date" type="date" value={dateTo} onChange={(e) => { setDateTo(e.target.value); setPage(0); }} className="mt-1 block h-9 rounded border border-outline-variant bg-surface px-2" /></label><label className="text-xs text-on-surface-variant">Status<select value={status} onChange={(e) => { setStatus(e.target.value as AppointmentStatus | ""); setPage(0); }} className="mt-1 block h-9 rounded border border-outline-variant bg-surface px-2"><option value="">All statuses</option>{statuses.map((item) => <option key={item}>{item}</option>)}</select></label><label className="text-xs text-on-surface-variant">Visit type<select value={visitType} onChange={(e) => { setVisitType(e.target.value as VisitType | ""); setPage(0); }} className="mt-1 block h-9 rounded border border-outline-variant bg-surface px-2"><option value="">All visit types</option>{visitTypes.map((item) => <option key={item}>{item.replaceAll("_", " ")}</option>)}</select></label></section>{isLoading ? <section className="rounded-lg border border-outline-variant bg-surface-container-lowest p-6 text-sm text-on-surface-variant">Loading appointment schedule…</section> : isError ? <section className="rounded-lg border border-error/30 bg-error-container/30 p-4 text-sm text-on-error-container">Unable to load appointments. Please refresh and try again.</section> : <AppointmentTable items={items} />}<section className="flex flex-wrap items-center justify-between gap-2 rounded-lg border border-outline-variant bg-surface-container-lowest p-3 text-xs"><span className="text-on-surface-variant">Page {page + 1} of {Math.max(totalPages, 1)} • {data?.totalElements || 0} records</span><div className="flex items-center gap-2"><select value={size} onChange={(e) => { setSize(Number(e.target.value)); setPage(0); }} aria-label="Rows per page" className="h-8 rounded border border-outline-variant bg-surface px-2"><option value={20}>20</option><option value={50}>50</option><option value={100}>100</option></select><button disabled={page === 0} onClick={() => setPage((value) => Math.max(0, value - 1))} className="h-8 rounded border border-outline-variant px-2 disabled:opacity-40">Previous</button><button disabled={page + 1 >= totalPages} onClick={() => setPage((value) => value + 1)} className="h-8 rounded border border-outline-variant px-2 disabled:opacity-40">Next</button></div></section></div>;
}
